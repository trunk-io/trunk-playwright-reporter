{
  "name": "trunk-reporter",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "target-tests": "cd test/target && npx playwright test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "@playwright/test": "^1.54.2",
    "typescript": "^5"
  },
  "dependencies": {
    "junit-report-builder": "^5.1.1"
  }
}
